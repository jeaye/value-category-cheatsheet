Subscript operation on an lvalue array results in an lvalue.

§ 5.2.1 (1) The expression `E1[E2]` is identical (by definition) to `*((E1)+(E2))`, except that, in the case of an array operand, the result is an lvalue if that operand is an lvalue and an xvalue otherwise.
