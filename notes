TODO:
  - Add a section on forwarding references
  - Add more patterns
  - Invalid code, such as prvalue -> non-const lvalue binding

Patterns:
  --
  Return a struct and try to keep a ref to a member
  Return a container and try to keep a ref to an element
