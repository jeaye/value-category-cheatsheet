LIFETIME EXTENSION

--

struct T{};
T const &ref{ T{} }; // lvalue

--

struct T{};
T &&ref{ T{} }; // lvalue

--

struct T{};
T &&ref{ std::move(T{}) }; // undefined behavior

--

int &&a{ 5 }; // lvalue
int const &b{ std::move(a) }; // lvalue; does not prolong lifetime
